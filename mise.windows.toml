# Windows overrides
# Requires: Visual Studio C++ workload OR MinGW-w64, Developer PowerShell
# fxc.exe available natively via Windows SDK (no Wine needed)

[env]
CPM_SOURCE_CACHE = "{{ env.USERPROFILE }}/.cache/CPM"
